broker:
        brokerSecret: "${name}-${broker_provider}-broker"
        brokerProviders: "${broker_providers}"
        brokerServersSuffix: "broker_servers"
        brokerPasswordKeySuffix: "broker_password"
        brokerTopicsKeySuffix: "broker_topics"