---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ${name}-${broker_provider}-broker
  namespace: ${namespace}-ga
spec:
  encryptedData:${broker_properties_ga}
  template:
    data: ${broker_template_ga}