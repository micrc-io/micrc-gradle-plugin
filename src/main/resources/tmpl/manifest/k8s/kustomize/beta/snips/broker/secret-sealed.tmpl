---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ${name}-${broker_provider}-broker
  namespace: ${namespace}-beta
spec:
  encryptedData:${broker_properties_beta}
  template:
    data: ${broker_template_beta}