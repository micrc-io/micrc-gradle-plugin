---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ${name}-${broker_provider}-broker
  namespace: ${namespace}-release
spec:
  encryptedData:${broker_properties_release}
  template:
    data: ${broker_template_release}